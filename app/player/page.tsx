"use client";

import Link from "next/link";
import { useEffect, useMemo, useState } from "react";

type Availability = { eventId: string; playerId: string; status: string };
type Feedback = { id: string; wentWell?: string | null; needsWork?: string | null; focusNext?: string | null; passing?: number | null; firstTouch?: number | null; positioning?: number | null; confidence?: number | null; createdAt: string };
type Player = { id: string; firstName: string; lastName: string; position: string; availability: Availability[]; feedback: Feedback[] };
type Event = { id: string; type: "MATCH" | "TRAINING"; title: string; opponent?: string | null; startsAt: string; arrivalTime?: string | null; venue: string; instructions?: string | null; availability: Availability[]; selections: { playerId: string; role: string; position?: string | null; isCaptain: boolean }[] };
type Data = { player: Player; events: Event[]; messages: { id: string; sender: string; body: string; createdAt: string }[]; team?: { name: string; ageGroup: string; club: string } };
const fmt = (value: string) => new Date(value).toLocaleString("en-GB", { weekday: "short", day: "numeric", month: "short", hour: "2-digit", minute: "2-digit" });

export default function PlayerPage() {
  const [data, setData] = useState<Data | null>(null); const [saving, setSaving] = useState(""); const [message, setMessage] = useState("");
  const load = async () => { const res = await fetch("/api/coachhub/player", { cache: "no-store" }); const json = await res.json(); if (res.ok) { setData(json); setMessage(""); } else setMessage(json.error || "Player access required"); };
  useEffect(() => { load(); }, []);
  const player = data?.player;
  const events = useMemo(() => [...(data?.events || [])].filter((e) => new Date(e.startsAt) >= new Date()).slice(0, 8), [data]);
  const feedback = player?.feedback?.[0];
  const setAvailability = async (event: Event, status: "AVAILABLE" | "UNAVAILABLE") => { if (!player || saving) return; setSaving(event.id + status); setMessage(""); const res = await fetch("/api/coachhub/player", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: "availability", eventId: event.id, status }) }); const json = await res.json().catch(() => ({})); if (res.ok) { setMessage("Availability updated."); await load(); } else setMessage(json.error || "Could not update availability."); setSaving(""); };
  return <main className="min-h-screen bg-slate-950 text-white p-4 sm:p-8"><div className="mx-auto max-w-3xl space-y-6"><header className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-sm font-bold text-blue-400">COACHHUB · PLAYER</p><h1 className="text-3xl font-black">{player ? `${player.firstName} ${player.lastName}` : "Player view"}</h1><p className="text-slate-400">Your schedule, availability and development.</p></div><Link href="/" className="rounded-xl border border-slate-700 px-4 py-2 text-sm font-bold">Team home</Link></header>{message && <div className="rounded-xl border border-blue-800 bg-blue-950/40 p-3 text-sm font-bold text-blue-200">{message}</div>}{player && <section className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><p className="text-xs font-black tracking-widest text-blue-400">YOUR PROFILE</p><div className="mt-3 flex items-center gap-4"><div className="grid h-14 w-14 place-items-center rounded-full bg-blue-600 text-lg font-black">{player.firstName[0]}{player.lastName[0]}</div><div><h2 className="text-2xl font-black">{player.firstName} {player.lastName}</h2><p className="text-slate-400">{player.position}{data?.team ? ` · ${data.team.name}` : ""}</p></div></div></section>}<section><h2 className="mb-3 text-xl font-black">Availability</h2><div className="space-y-3">{events.map((event) => { const a = player?.availability.find((x) => x.eventId === event.id); const selection = event.selections[0]; return <article key={event.id} className="rounded-2xl border border-slate-800 bg-slate-900 p-4"><div className="flex items-start justify-between gap-4"><div><span className="text-xs font-black uppercase text-blue-400">{event.type}</span><h3 className="text-lg font-black">{event.title}{event.opponent ? ` vs ${event.opponent}` : ""}</h3><p className="text-sm text-slate-400">{fmt(event.startsAt)} · {event.venue}</p>{event.arrivalTime && <p className="mt-1 text-sm text-slate-500">Arrival: {fmt(event.arrivalTime)}</p>}{event.instructions && <p className="mt-2 text-sm text-slate-300">{event.instructions}</p>}{selection && <p className="mt-2 text-sm font-bold text-emerald-300">Squad: {selection.role.replace("_", " ")}{selection.position ? ` · ${selection.position}` : ""}{selection.isCaptain ? " · Captain" : ""}</p>}</div><span className="rounded-full bg-slate-800 px-3 py-1 text-xs font-bold">{a?.status || "PENDING"}</span></div><div className="mt-4 grid grid-cols-2 gap-3"><button disabled={!!saving} onClick={() => setAvailability(event, "AVAILABLE")} className="rounded-xl bg-emerald-600 px-2 py-4 text-sm font-black">Available</button><button disabled={!!saving} onClick={() => setAvailability(event, "UNAVAILABLE")} className="rounded-xl bg-rose-600 px-2 py-4 text-sm font-black">Not available</button></div></article>; })}</div></section>{feedback && <section className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="text-xl font-black">Latest feedback</h2><div className="mt-4 grid gap-3 sm:grid-cols-4">{[["Passing", feedback.passing],["First touch", feedback.firstTouch],["Positioning", feedback.positioning],["Confidence", feedback.confidence]].map(([label, value]) => <div key={String(label)} className="rounded-xl bg-slate-950 p-3"><p className="text-xs text-slate-500">{label}</p><p className="text-2xl font-black">{value ?? "—"}<span className="text-sm text-slate-600">/5</span></p></div>)}</div>{feedback.wentWell && <p className="mt-4"><b>Went well:</b> {feedback.wentWell}</p>}{feedback.needsWork && <p className="mt-2"><b>Needs work:</b> {feedback.needsWork}</p>}{feedback.focusNext && <p className="mt-2 text-blue-300"><b>Next focus:</b> {feedback.focusNext}</p>}</section>}<section className="rounded-2xl border border-slate-800 bg-slate-900 p-5"><h2 className="text-xl font-black">Team messages</h2><div className="mt-3 space-y-3">{data?.messages?.slice(0, 5).map((m) => <div key={m.id} className="rounded-xl bg-slate-950 p-3"><p className="text-xs font-bold text-slate-500">{m.sender} · {fmt(m.createdAt)}</p><p className="mt-1">{m.body}</p></div>)}</div></section></div></main>;
}
